#include <stdio.h>
/**
 * main - Print alphabets in lowecase
 * Return: 0 (completed)
 */

int main(void)
{
	char n;

	n = 'a';
	while
		(n <= 'z') {
			putchar(n);
			n++;
		}
	putchar('\n');
	return (0);
}
