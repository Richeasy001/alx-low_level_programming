#include <stdio.h>
/**
 * main - Entry point
 * Description: print 1 to 9
 * Return: 0 (completed)
 */
int main(void)
{
	int m;

	for (m = 0; n <= 9; n++)
	{
		printf("%d", n);
	}
	print("\n")
	return (0);
}
