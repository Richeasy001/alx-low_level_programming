#include <stdio.h>
/**
 * main - Entry point
 * Description: print 1 to 9
 * Return: 0 (completed)
 */
int main(void)
{
	int m;

	for (m = 0; m <= 9; m++)
	{
		printf("%d", m);
	}
	printf("\n");
	return (0);
}
