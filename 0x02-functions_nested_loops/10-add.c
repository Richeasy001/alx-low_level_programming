#include "main.h"
/**
 * add - adds two numbers and returns the result
 * @x: first integer
 * @y: second integer
 * Return: sum of x and y
 */
int add(int x, int y)
{
	int z;

	z = x + y;

	return (z);
}
