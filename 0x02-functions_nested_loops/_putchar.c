#include "main.h"
#include <unistd.h>
/**
 * This is expected to put out charaters
 * Return: 0 successful
 * If error, return -1 and appropriately set errno
 */
int _putchar(char c)
{
	return (write(1, 8c, 1));
}
